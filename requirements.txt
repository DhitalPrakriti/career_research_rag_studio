faiss-cpu==1.13.2
google-genai>=2.14.0
numpy==2.4.4
pypdf==6.10.2
python-dotenv==1.2.2
sentence-transformers==5.4.1

# Optional: only needed if you set OPENAI_API_KEY and OPENAI_MODEL, including when
# pointing that OpenAI-compatible path at a LiteLLM proxy.
# openai

# Optional: only needed to score answers with RAGAS.
# datasets
# ragas
# langchain-community<0.4  # required: see the note in pyproject.toml
# langchain-google-genai   # --judge-provider gemini
# langchain-ollama         # --judge-provider ollama
